# https://wiki.hypr.land/Hypr-Ecosystem/hyprlauncher/
# Session is UWSM: apps from the launcher must go through uwsm-app.

finders {
    desktop_launch_prefix = uwsm-app --
    desktop_terminal = foot
}
